# Shift Trading Modernization - Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                        BROWSER (Client)                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    React Application                         │  │
│  ├──────────────────────────────────────────────────────────────┤  │
│  │                                                               │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  App.tsx / App-modern.tsx                             │  │  │
│  │  │  • Route configuration                                │  │  │
│  │  │  • Theme providers                                    │  │  │
│  │  │  • Auth state management                              │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  │                         ↓                                      │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  ModernLayout (modern-layout.tsx)                     │  │  │
│  │  │  • Responsive sidebar (desktop)                       │  │  │
│  │  │  • Hamburger menu (mobile)                            │  │  │
│  │  │  • Navigation routing                                 │  │  │
│  │  │  • User profile menu                                  │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  │                         ↓                                      │  │
│  │  ┌────────────────────────────────────────────────────────┐  │  │
│  │  │  ShiftTradingPanel (shift-trading-panel.tsx)          │  │  │
│  │  │  • Trade list view (tabs)                             │  │  │
│  │  │  • Create trade dialog                                │  │  │
│  │  │  • Accept/reject actions                              │  │  │
│  │  │  • Manager approval view                              │  │  │
│  │  │  • Trade cards with animations                        │  │  │
│  │  └────────────────────────────────────────────────────────┘  │  │
│  │                                                               │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │           React Query (Data Management)                     │  │
│  │  ┌─────────────────────────────────────────────────────┐   │  │
│  │  │  Query Hooks:                                       │   │  │
│  │  │  • useQuery('shift-trades', GET /api/shift-trades) │   │  │
│  │  │  • useQuery('employee-shifts', GET /api/shifts)    │   │  │
│  │  │  • useQuery('employees', GET /api/employees)       │   │  │
│  │  │                                                     │   │  │
│  │  │  Mutation Hooks:                                    │   │  │
│  │  │  • createTradeMutation (POST)                       │   │  │
│  │  │  • respondToTradeMutation (PATCH)                   │   │  │
│  │  │  • approveTradeAsManagerMutation (PATCH)            │   │  │
│  │  └─────────────────────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │        Real-Time Updates (use-realtime.ts hook)             │  │
│  │  ┌─────────────────────────────────────────────────────┐   │  │
│  │  │  Socket.IO Client                                   │   │  │
│  │  │  • Connected? → Use WebSocket                       │   │  │
│  │  │  • Not available? → Fallback to polling             │   │  │
│  │  │                                                     │   │  │
│  │  │  Event Listeners:                                   │   │  │
│  │  │  • shift:created → Invalidate queries               │   │  │
│  │  │  • shift:updated → Invalidate queries               │   │  │
│  │  │  • trade:created → Invalidate queries               │   │  │
│  │  │  • trade:status-changed → Invalidate queries        │   │  │
│  │  │  • availability:updated → Invalidate queries        │   │  │
│  │  └─────────────────────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ HTTP/WS
                    ┌─────────────────────┐
                    │  Network (Internet) │
                    └─────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      Node.js Server (Express)                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Express App (server/index.ts)                             │  │
│  │  • HTTP request handling                                   │  │
│  │  • Route middleware                                        │  │
│  │  • CORS configuration                                      │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                         ↓                                            │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Socket.IO Server (realtime-manager.ts)                   │  │
│  │  ┌─────────────────────────────────────────────────────┐   │  │
│  │  │  Connection Management:                             │   │  │
│  │  │  • Track user connections                           │   │  │
│  │  │  • Create user-specific rooms                        │   │  │
│  │  │  • Handle disconnections                             │   │  │
│  │  │                                                     │   │  │
│  │  │  Event Broadcasting:                                │   │  │
│  │  │  • broadcastShiftCreated(shift)                      │   │  │
│  │  │  • broadcastShiftUpdated(shift)                      │   │  │
│  │  │  • broadcastTradeCreated(trade)                      │   │  │
│  │  │  • broadcastTradeStatusChanged(id, status, trade)   │   │  │
│  │  │  • notifyAvailabilityUpdate(employeeId, data)       │   │  │
│  │  └─────────────────────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                         ↓                                            │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  API Routes (server/routes.ts)                             │  │
│  │  ┌─────────────────────────────────────────────────────┐   │  │
│  │  │  Shift Trades Endpoints:                            │   │  │
│  │  │  • POST /api/shift-trades                           │   │  │
│  │  │    └─ realtime.broadcastTradeCreated(newTrade)     │   │  │
│  │  │                                                     │   │  │
│  │  │  • PATCH /api/shift-trades/:id                      │   │  │
│  │  │    └─ realtime.broadcastTradeStatusChanged(...)    │   │  │
│  │  │                                                     │   │  │
│  │  │  • PATCH /api/shift-trades/:id/approve              │   │  │
│  │  │    └─ realtime.broadcastTradeStatusChanged(...)    │   │  │
│  │  │    └─ realtime.broadcastShiftUpdated(...)           │   │  │
│  │  │                                                     │   │  │
│  │  │  • PUT /api/shift-trades/:id/take                   │   │  │
│  │  │    └─ realtime.broadcastTradeStatusChanged(...)    │   │  │
│  │  │                                                     │   │  │
│  │  │  • DELETE /api/shift-trades/:id                     │   │  │
│  │  │    └─ no event (just invalidate on client)          │   │  │
│  │  └─────────────────────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                         ↓                                            │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Database Layer (Drizzle ORM)                             │  │
│  │  ┌─────────────────────────────────────────────────────┐   │  │
│  │  │  Tables:                                            │   │  │
│  │  │  • users (id, email, role, ...)                     │   │  │
│  │  │  • shifts (id, userId, date, startTime, ...)        │   │  │
│  │  │  • shiftTrades (id, shiftId, fromUserId, toUserId,  │   │  │
│  │  │               status, urgency, ...)                 │   │  │
│  │  └─────────────────────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                         ↓                                            │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  PostgreSQL Database (Neon)                               │  │
│  │  • Persistent data storage                                │  │
│  │  • Transaction support                                    │  │
│  │  • ACID compliance                                        │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

### Creating a Shift Trade

```
┌─────────────────────────────────────────────────────────────────────┐
│ User clicks "New Request" button in ShiftTradingPanel              │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Dialog opens with form (shift selection, target employee, reason)  │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────────┐
│ User fills form and clicks "Create Request"                        │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────────┐
│ createTradeMutation.mutate() triggered                             │
│ POST /api/shift-trades {shiftId, targetUserId, reason, urgency}   │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
┌──────────────────────────────────┬──────────────────────────────────┐
│ Server receives request           │ Browser sends request            │
│ Creates trade in database         │ Shows loading spinner            │
│ Calls realtime.                   │                                  │
│   broadcastTradeCreated(newTrade) │                                  │
└──────────────────────────┬────────┴──────────────────────────────────┘
                           ↓
┌──────────────────────────────────┬──────────────────────────────────┐
│ Socket.IO server emits           │ Browser receives event           │
│ "trade:created" event to:         │ (via WebSocket or polling)      │
│ • fromUserId room                 │                                  │
│ • toUserId room                   │ React Query invalidates:         │
│ • managers room                   │ • useQuery('shift-trades')      │
└──────────────────────────┬────────┴──────────────────────────────────┘
                           ↓
┌──────────────────────────────────┬──────────────────────────────────┐
│ All connected clients receive     │ Browser refetches data           │
│ event and see:                    │ from GET /api/shift-trades       │
│ "📨 New trade request" log        │                                  │
│ in console                        │ Updated list displays in UI      │
└──────────────────────────────────┴──────────────────────────────────┘
```

### Accepting a Shift Trade (Real-Time)

```
┌─────────────────────────────────────────────────────────────────────┐
│ User in Window 2 sees trade in "Incoming" tab                       │
│ Clicks "Accept" button                                              │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────────┐
│ respondToTradeMutation.mutate({ tradeId, accept: true })           │
│ PATCH /api/shift-trades/:id {status: "accepted"}                   │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
┌──────────────────────────────────┬──────────────────────────────────┐
│ Server receives request           │ Browser shows loading state      │
│ Updates trade to "accepted"       │                                  │
│ Calls realtime.                   │                                  │
│   broadcastTradeStatusChanged()   │                                  │
└──────────────────────────┬────────┴──────────────────────────────────┘
                           ↓
┌──────────────────────────────────┬──────────────────────────────────┐
│ Socket.IO emits                   │ WINDOW 2:                        │
│ "trade:status-changed" to:        │ ✅ Button changes to spinner    │
│ • fromUserId (creator)            │ ✅ Refetch triggered            │
│ • toUserId (acceptor)             │ ✅ Trade moves to accepted      │
│ • managers                         │                                  │
│                                    │ WINDOW 1 (creator):            │
│                                    │ ✅ Automatic refetch           │
│                                    │ ✅ Status badge updates        │
│                                    │ ✅ "📝 Trade accepted" log     │
└──────────────────────────────────┴──────────────────────────────────┘
```

---

## Component Hierarchy

```
App.tsx (or App-modern.tsx)
│
├─ ThemeProvider
│  └─ MuiThemeProvider
│     └─ QueryClientProvider
│        └─ TooltipProvider
│           └─ AppRoutes
│              │
│              ├─ [Public Routes]
│              │  ├─ MuiLogin
│              │  └─ Setup
│              │
│              └─ [Private Routes]
│                 └─ ModernLayout
│                    │
│                    ├─ [Sidebar]
│                    │  ├─ Logo
│                    │  ├─ Navigation Items
│                    │  │  ├─ Dashboard
│                    │  │  ├─ ShiftTrading ⭐
│                    │  │  ├─ Schedule
│                    │  │  ├─ TimeOff
│                    │  │  ├─ Employees
│                    │  │  ├─ Payroll
│                    │  │  └─ Notifications
│                    │  └─ User Profile Menu
│                    │
│                    ├─ [Top App Bar]
│                    │  ├─ Hamburger Menu (mobile)
│                    │  ├─ Page Title
│                    │  └─ User Avatar Menu
│                    │
│                    └─ [Page Content]
│                       └─ ShiftTradingPanel ⭐
│                          │
│                          ├─ Header
│                          │  ├─ Title & Description
│                          │  └─ New Request Button
│                          │
│                          ├─ Tabs
│                          │  ├─ My Requests Tab
│                          │  │  └─ TradeCard[] (outgoing)
│                          │  ├─ Incoming Tab
│                          │  │  └─ TradeCard[] (incoming)
│                          │  └─ Approvals Tab (managers)
│                          │     └─ TradeCard[] (pending)
│                          │
│                          └─ Create Trade Dialog
│                             ├─ Shift Select
│                             ├─ Employee Select
│                             ├─ Urgency Select
│                             ├─ Reason Text Field
│                             └─ Action Buttons

⭐ = Enhanced/New with modernization
```

---

## Real-Time Event Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                     Client-Side WebSocket Flow                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  useRealtime Hook (on mount):                                       │
│  ↓                                                                   │
│  io({                                                               │
│    query: { userId: currentUser.id },                               │
│    auth: { token: auth_token }                                      │
│  })                                                                  │
│  ↓                                                                   │
│  socket.on('connect', () => {                                       │
│    emit('subscribe:employee-shifts')                                │
│    emit('subscribe:shift-trades')                                   │
│    console.log('✅ Connected to real-time updates')                 │
│  })                                                                  │
│  ↓                                                                   │
│  Listening for events:                                              │
│  │                                                                   │
│  ├─ socket.on('shift:created', (data) => {                          │
│  │    queryClient.invalidateQueries(['employee-shifts'])            │
│  │    console.log('📍 New shift created:', data)                    │
│  │  })                                                               │
│  │                                                                   │
│  ├─ socket.on('shift:updated', (data) => {                          │
│  │    queryClient.invalidateQueries(['employee-shifts'])            │
│  │    console.log('🔄 Shift updated:', data)                        │
│  │  })                                                               │
│  │                                                                   │
│  ├─ socket.on('trade:created', (data) => {                          │
│  │    queryClient.invalidateQueries(['shift-trades'])               │
│  │    console.log('📨 New trade request:', data)                    │
│  │  })                                                               │
│  │                                                                   │
│  ├─ socket.on('trade:status-changed', (data) => {                   │
│  │    queryClient.invalidateQueries(['shift-trades'])               │
│  │    console.log('📝 Trade status changed:', data)                 │
│  │  })                                                               │
│  │                                                                   │
│  └─ socket.on('availability:updated', (data) => {                   │
│       queryClient.invalidateQueries(['employees'])                  │
│       console.log('👥 Availability updated:', data)                 │
│     })                                                               │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## WebSocket Fallback Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│ Client initiates connection attempt                                 │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────────┐
│ Try WebSocket first (socket.io protocol)                            │
│ If available and connected → Use WebSocket (instant updates)        │
│ If blocked by proxy/firewall → Fall back to polling                 │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────────┐
│ POLLING FALLBACK (if WebSocket unavailable):                        │
│ • Client stores last query state                                    │
│ • Every 5 seconds → GET /api/shift-trades                           │
│ • Compare with stored state                                         │
│ • If different → Update UI                                          │
│ • If same → Skip UI update                                          │
│ • No console warnings (silent fallback)                             │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Database Schema Relationships

```
┌──────────────────────────────────────────────────────────────────┐
│                         users table                              │
├──────────────────────────────────────────────────────────────────┤
│  PK: id (uuid)                                                   │
│  Fields: email, firstName, lastName, role, ...                   │
│                                                                  │
│  Relationships:                                                  │
│  ├─← shifts.userId (one user has many shifts)                   │
│  ├─← shiftTrades.fromUserId (creator of trade)                  │
│  └─← shiftTrades.toUserId (acceptor of trade)                   │
└──────────────────────────────────────────────────────────────────┘
                           ↑
                           │
        ┌──────────────────┴──────────────────┐
        │                                      │
┌───────▼──────────────────┐  ┌──────────────▼──────────────────┐
│    shifts table          │  │   shiftTrades table            │
├──────────────────────────┤  ├────────────────────────────────┤
│  PK: id (uuid)           │  │  PK: id (uuid)                 │
│  FK: userId → users.id   │  │  FK: shiftId → shifts.id       │
│  Fields:                 │  │  FK: fromUserId → users.id     │
│  • date                  │  │  FK: toUserId → users.id       │
│  • startTime             │  │  FK: approvedBy → users.id     │
│  • endTime               │  │                                │
│  • position              │  │  Fields:                       │
│  • createdAt             │  │  • reason (text)               │
│  • updatedAt             │  │  • status (enum)               │
│                          │  │  • urgency (enum)              │
│                          │  │  • requestedAt                 │
│                          │  │  • approvedAt                  │
│                          │  │  • createdAt                   │
│                          │  │  • updatedAt                   │
└──────────────────────────┘  └────────────────────────────────┘
        ▲                             ▲
        │                             │
        └─────── Trade references ────┘
           (shiftId links shift to trade)
```

---

## State Management Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                     React Query Cache Layer                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  Query Keys:                                                        │
│  ├─ ['shift-trades']        → useQuery(GET /api/shift-trades)      │
│  ├─ ['employee-shifts']     → useQuery(GET /api/shifts)            │
│  └─ ['employees']           → useQuery(GET /api/employees)         │
│                                                                      │
│  Auto-Refetch Triggers:                                             │
│  ├─ On component mount → staleTime: 0 (immediately stale)          │
│  ├─ On window focus → refetchOnWindowFocus: true                   │
│  ├─ Every 3 seconds → refetchInterval: 3000 (polling)              │
│  └─ On WebSocket event → queryClient.invalidateQueries()           │
│                                                                      │
│  Mutation Triggers:                                                 │
│  ├─ POST /api/shift-trades → createTradeMutation                   │
│  ├─ PATCH /api/shift-trades/:id → respondToTradeMutation           │
│  └─ PATCH /api/shift-trades/:id/approve → approveMutation         │
│       ↓                                                              │
│       Invalidate ['shift-trades'] cache                             │
│       ↓                                                              │
│       Refetch immediately                                           │
│       ↓                                                              │
│       UI updates with new data                                      │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Your Machine                                │
│  npm run build                                                      │
│  ├─ Vite: client/src → dist/client/                                │
│  ├─ Esbuild: server/ → dist/index.js                               │
│  └─ Creates: dist/ (ready to deploy)                               │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    GitHub Repository                               │
│  git push origin main                                               │
│  └─ Sends dist/ to GitHub                                           │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    Render Deployment                                │
│  • Watches GitHub for pushes                                        │
│  • Pulls latest code                                                │
│  • npm install (gets socket.io, etc.)                               │
│  • npm run build (if specified)                                     │
│  • npm run start (or node dist/index.js)                            │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Production Server                                           │  │
│  │  • Listens on HTTPS port 443                                 │  │
│  │  • Routes HTTP to /api/* endpoints                           │  │
│  │  • Routes WebSocket to Socket.IO                             │  │
│  │  • Serves static files (React build)                         │  │
│  │  • Connected to PostgreSQL (Neon)                            │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
└──────────────────────────┬──────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────────┐
│                  Client Browsers (Users)                            │
│  • GET https://your-app.onrender.com                               │
│  • Receives React app (bundled)                                     │
│  • Connects WebSocket to Socket.IO server                           │
│  • Sends requests to /api/* endpoints                               │
│  • Receives real-time updates via socket                            │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Technology Stack Visual

```
┌──────────────────────────────────────────────────────────────────────┐
│                          Frontend (React)                            │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  React 18.3.1                                                       │
│  ├─ Components: JSX-based UI                                        │
│  ├─ Hooks: useState, useEffect, useQuery, useMutation               │
│  └─ Context: ThemeProvider, TooltipProvider                         │
│                                                                      │
│  Material-UI 7.3.6 (MUI)                                            │
│  ├─ Components: Card, Button, Dialog, Tabs, etc.                   │
│  ├─ Theme: Light/Dark mode, Color system                            │
│  └─ Icons: @mui/icons-material                                      │
│                                                                      │
│  React Query 5.60.5                                                 │
│  ├─ Caching: Automatic query deduplication                          │
│  ├─ Polling: Refetch intervals                                      │
│  └─ Mutations: Handle POST/PATCH/PUT                                │
│                                                                      │
│  Socket.IO Client 4.8.1                                             │
│  ├─ WebSocket: Real-time bidirectional communication                │
│  ├─ Fallback: Long polling if needed                                │
│  └─ Events: Trade, shift, availability updates                      │
│                                                                      │
│  TypeScript 5.6.3                                                   │
│  ├─ Type Safety: Interfaces, types for all data                     │
│  ├─ Compile-time Errors: Catch bugs before runtime                  │
│  └─ IDE Support: Better autocomplete and refactoring                │
│                                                                      │
│  Build Tool: Vite 7.2.6                                             │
│  └─ Lightning fast HMR, optimized production bundles                │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│                         Backend (Node.js)                            │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  Express 4.21.2                                                     │
│  ├─ Server: HTTP API handling                                       │
│  ├─ Middleware: Auth, CORS, session                                 │
│  └─ Routes: /api/shift-trades, /api/shifts, etc.                    │
│                                                                      │
│  Socket.IO Server 4.8.1                                             │
│  ├─ Real-time: Bidirectional communication                          │
│  ├─ Events: Broadcast to clients                                    │
│  └─ Rooms: User-specific event channels                             │
│                                                                      │
│  Drizzle ORM 0.39.1                                                 │
│  ├─ Database: Type-safe SQL queries                                 │
│  ├─ Migrations: Schema versioning                                   │
│  └─ Relationships: Foreign keys, joins                              │
│                                                                      │
│  PostgreSQL (Neon)                                                  │
│  ├─ Storage: users, shifts, shiftTrades tables                      │
│  ├─ Transactions: ACID compliance                                   │
│  └─ Serverless: Auto-scaling, managed                               │
│                                                                      │
│  TypeScript 5.6.3                                                   │
│  └─ Type Safety: Compile-time error checking                        │
│                                                                      │
│  Build Tool: Esbuild                                                │
│  └─ Fast: Bundles Node.js code for Render                           │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│                      Deployment (Render)                             │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  Render.com                                                         │
│  ├─ Hosting: Node.js web service                                    │
│  ├─ Database: PostgreSQL (Neon backend)                             │
│  ├─ CI/CD: Auto-deploy on GitHub push                               │
│  ├─ Monitoring: Logs, metrics, alerts                               │
│  └─ Scale: Auto-scaling with load                                   │
│                                                                      │
│  GitHub                                                             │
│  ├─ Version Control: Git repository                                 │
│  ├─ CI/CD Trigger: GitHub Actions on push                           │
│  └─ Webhook: Notifies Render on code changes                        │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

This architecture diagram provides a comprehensive overview of how all the components work together to deliver real-time shift trading functionality.
